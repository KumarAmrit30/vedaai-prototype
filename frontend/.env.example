# REST API base URL (include /api suffix)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Socket.IO server URL (optional — defaults to API host without /api)
NEXT_PUBLIC_SOCKET_URL=http://localhost:8000
